# Claude Code project instructions (local only)
CLAUDE.md

# Design docs — PRDs, plans, requirements (internal only)
docs/design/

# Local-only scratch + internal dirs, consolidated here — never commit.
# Holds: *_PLAN.md scratch notes, animations/ (Remotion source), benchmarks/,
# diagrams/, presentations/, scripts/.
/.local/

# Backstop: any stray scratch planning markdown left at repo root (move it to .local/)
/*_PLAN.md

# Internal-only dirs kept at repo root (referenced by tracked config/docs/README)
demo/
grafana/

# Local website dev/debug helper scripts (not part of the site build)
website/scripts/

# Generated test reports
tests/TEST_REPORT.md

# Original nested directory (kept locally)
lakets/

# Build output
dist/

# Node modules — match directory AND symlink at any level
# (website/.gitignore's "node_modules/" trailing slash misses symlinks)
node_modules

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.ruff_cache/
.pytest_cache/
.venv/
.venv-*/
venv/
*.egg-info/

# Claude Code / Claude Terminal
.claude/
.claude-terminal

# DrawIO temp/backup files
.$*.drawio.bkp
.$*.drawio.dtmp

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.*

# GitHub Actions runner cache
_actions/
_PipelineMapping/
LakeTS/

# Visual comparison artifacts (Playwright screenshots)
.compare/
