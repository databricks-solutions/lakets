import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

// As docs are migrated from /docs in the repo root, add `Guides` and
// `Reference` categories with their pages here.
const sidebars: SidebarsConfig = {
  docsSidebar: ["intro"],
};

export default sidebars;
